$(function () {
   /* $('#newsButton').click(function () {
        document.location.href = "news";
        alert("news");
        this.hidden=true;
        $('#buttons').append('<button id="newsButton" type="button" class="btn btn-primary">Новости</button>');


    });
    $('#test').click(function () {
        // document.location.href = "news";
        // alert("news");
        // this.hidden=true;
        if (document.readyState === "complete") {$('#profileButton').toggle();}
        // this.setAttribute('id','news');


    });
    $('#profileButton').click(function () {
        if (document.readyState === "complete") {$('#test').toggle();}
        /!*
        this.hidden=true;
        $('#buttons').append('<button id="newsButton" type="button" class="btn btn-primary">Новости</button>')*!/
        // alert(this.hidden);
    });
*/
   // TODO а оно здесь надо?
})